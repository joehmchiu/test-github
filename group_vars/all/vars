ansible_python_interpreter: /usr/bin/python3
root: "{{ playbook_dir }}"
tag_script:  "'{{ root }}'/git-tag.pl"
user: "{{ mongo_user }}"
token: "{{ mongo_token }}"
username: "{{ git_user }}"
password: "{{ git_pass }}"
